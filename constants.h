#pragma once


// the maximum rack size in our game is 7 
const int RACK_SIZE = 7;

// the acsi of the first char in the bag 
const int  firstIdx = 'A';

// the top no. of racks estimated to be returned
const int maxReturnTiles = 150;

// max no of top remaining racks generated
const int maxTopRackRemained = 15;

// max no of top remaining racks generated
const int maxTopRackRefilled = 10;
